#!/usr/bin/env zsh
# ================================================================================================== # 
# Init and Config

eval "$(dircolors -b ~/.dir_colors)"

# ================================================================================================== #
# Alias
# TODO: These need completing

alias lsd="lsd --group-directories-first --sort git"

#alias ls="lsd"
alias ld="lsd -d"
alias la="lsd -a"
alias ll="lsd -l"
alias lla="lsd -la"
alias lt="lsd --tree --depth 2"

# ================================================================================================== #

